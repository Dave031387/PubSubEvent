namespace PubSubEvent
{
    /// <summary>
    /// The <see cref="EventManager" /> class manages event subscriptions and routes published
    /// events to the appropriate event handlers that were registered via the subscriptions.
    /// </summary>
    public class EventManager
    {
    }
}